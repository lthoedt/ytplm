const initialStateTrack = {

}

const trackActions = {
}

const TrackReducer = (state = initialStateTrack, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default TrackReducer;